import {{fileName}} from '../{{fileName}}';

export interface {{fileName}}ContainerProps {
  {{featureName}}: string;
  {{subName}}: string;
}

export function {{fileName}}Container(props: {{fileName}}ContainerProps) {
  return (
    <div>
      <h1>{{fullNameAsPascalCase}}</h1>
      <{{fileName}}>blah</{{fileName}}>
    </div>
  );
}