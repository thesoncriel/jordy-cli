import { ReactNode, memo } from 'react';
import styled from 'styled-components';
import { deepEqual } from 'jordy';

interface {{fileName}}Props {
  children?: ReactNode;
}

const Wrap = styled.div`

`;

function {{fileName}}Component({
  children,
}: {{fileName}}Props) {
  return (
    <Wrap>
      {children}
    </Wrap>
  );
};

/**
* @mixes React.memo
*/
export const {{fileName}} = memo(
  {{fileName}}Component,
  // deepEqual, // 필요하면 주석 해제하여 사용
  (prevProps, nextProps) => {
    return true;
});

{{fileName}}.displayName = '{{fileName}}';
